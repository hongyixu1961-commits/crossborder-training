"use client";
import Link from "next/link"; import {useEffect,useMemo,useState} from "react"; import {supabase} from "../../lib/supabase";
type Q={id:string;type:string;text:string;options:string[];answer:string;score:number;explanation?:string}; type Exam={id:string;title:string;description:string;duration_minutes:number;pass_score:number};
export default function Exams(){const [exam,setExam]=useState<Exam|null>(null),[questions,setQuestions]=useState<Q[]>([]),[answers,setAnswers]=useState<Record<string,string>>({}),[current,setCurrent]=useState(0),[submitted,setSubmitted]=useState(false),[loading,setLoading]=useState(true),[started,setStarted]=useState(false),[error,setError]=useState("");useEffect(()=>{async function load(){if(!supabase){setError("Supabase 未配置");setLoading(false);return}const {data:e,error:ee}=await supabase.from("exams").select("id,title,description,duration_minutes,pass_score").eq("published",true).order("created_at",{ascending:false}).limit(1).maybeSingle();if(ee||!e){setError(ee?.message||"暂无已发布考试");setLoading(false);return}const {data:qs,error:qe}=await supabase.from("questions").select("id,type,text,options,answer,score,explanation").eq("exam_id",e.id).order("position");if(qe)setError(qe.message);else{setExam(e);setQuestions((qs||[]) as Q[])}setLoading(false)}load()},[]);const q=questions[current];const score=useMemo(()=>questions.reduce((sum,x)=>sum+(answers[x.id]===x.answer?x.score:0),0),[answers,questions]);if(loading)return <main><section className="content"><p className="lead">正在加载考试题目…</p></section></main>;if(error||!exam)return <main><section className="content"><h1>考试中心</h1><p className="lead">{error||"暂无考试"}</p></section></main>;if(submitted)return <main><header><Link href="/" className="brand">✦ CrossBorder <b>Academy</b></Link></header><section className="content"><p className="eyebrow">EXAM RESULT</p><h1>考试完成</h1><div className="exam-result"><strong>{score}</strong><span>/ {questions.reduce((s,x)=>s+x.score,0)} 分</span><p>{score>=exam.pass_score?"恭喜你，考试通过！":"本次未通过，可以复习后再次参加。"}</p><button className="cta" onClick={()=>{setSubmitted(false);setAnswers({});setCurrent(0);setStarted(false)}}>重新考试</button></div></section></main>;return <main><header><Link href="/" className="brand">✦ CrossBorder <b>Academy</b></Link><nav><Link href="/learning">学习中心</Link><a>考试中心</a></nav></header><section className="content">{!started?<><p className="eyebrow">EXAM CENTER</p><h1>{exam.title}</h1><p className="lead">{exam.description}</p><div className="exam-card"><p>共 {questions.length} 题 · 满分 {questions.reduce((s,x)=>s+x.score,0)} 分 · 建议用时 {exam.duration_minutes} 分钟</p><p>及格分：{exam.pass_score} 分</p><button className="cta" onClick={()=>setStarted(true)}>开始考试 →</button></div></>:<div className="single-question"><div className="exam-progress"><span>第 {current+1} 题 / 共 {questions.length} 题</span><b>{Math.round(((current+1)/questions.length)*100)}%</b></div><div className="question-card"><div className="question-meta"><span>题型：{q.type}</span><span>分值：{q.score} 分</span></div><h1>{q.text}</h1><div className="answer-options">{q.options.map((o,j)=>{const letter=String.fromCharCode(65+j);return <label key={o} className={answers[q.id]===letter?"answer-option chosen":"answer-option"}><input type="radio" name={q.id} checked={answers[q.id]===letter} onChange={()=>setAnswers({...answers,[q.id]:letter})}/><span>{letter}</span>{o}</label>})}</div></div><div className="question-actions"><button disabled={current===0} onClick={()=>setCurrent(current-1)}>← 上一题</button>{current<questions.length-1?<button className="cta" disabled={!answers[q.id]} onClick={()=>setCurrent(current+1)}>下一题 →</button>:<button className="cta" disabled={Object.keys(answers).length<questions.length} onClick={()=>setSubmitted(true)}>提交试卷</button>}</div></div>}</section></main>}

