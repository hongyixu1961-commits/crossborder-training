"use client";
import Link from "next/link"; import {ChangeEvent,useState} from "react";
export default function QuestionsAdmin(){const [rows,setRows]=useState<string[][]>([]);function upload(e:ChangeEvent<HTMLInputElement>){const file=e.target.files?.[0];if(!file)return;const reader=new FileReader();reader.onload=()=>{const text=String(reader.result||"");setRows(text.split(/\r?\n/).filter(Boolean).map(line=>line.split(","))) };reader.readAsText(file)}function download(){const blob=new Blob(["题目类型,题目内容,选项A,选项B,选项C,选项D,正确答案,分值,解析,所属课程\n单选题,示例题目,选项A,选项B,选项C,选项D,B,5,填写解析,Amazon运营基础"],{type:"text/csv;charset=utf-8"});const a=document.createElement("a");a.href=URL.createObjectURL(blob);a.download="exam-question-template.csv";a.click()}return <main><header><Link href="/" className="brand">✦ CrossBorder <b>Academy</b></Link><nav><Link href="/learning">学习中心</Link><Link href="/exams">考试中心</Link></nav></header><section className="content"><p className="eyebrow">QUESTION BANK</p><h1>题库管理</h1><p className="lead">使用 CSV 批量导入考试题目。</p><div className="upload-card"><button className="cta" onClick={download}>下载 CSV 模板</button><label className="upload-label">选择 CSV 文件<input type="file" accept=".csv,text/csv" onChange={upload}/></label><p>字段：题目类型、题目内容、选项 A-D、正确答案、分值、解析、所属课程。</p></div>{rows.length>0&&<div className="table-preview"><h2>导入预览（{rows.length-1} 道题）</h2>{rows.slice(0,6).map((r,i)=><div className="csv-row" key={i}>{r.slice(0,4).join("　")}</div>)}<button className="cta" onClick={()=>alert("演示导入完成；连接 Supabase 后将写入题库表。")}>确认导入</button></div>}</section></main>}

